<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeuFilme"
        android:usesCleartextTraffic="true">

        <!-- SplashActivity: tela inicial, ponto de entrada do app -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FormularioActivity: tela de perguntas -->
        <activity
            android:name=".FormularioActivity"
            android:exported="false"/>

        <!-- ResultadosActivity: tela de recomendações -->
        <activity
            android:name=".ResultadosActivity"
            android:exported="false"/>

        <!-- HistoricoActivity: tela de histórico -->
        <activity
            android:name=".HistoricoActivity"
            android:exported="false"/>

        <!-- DetalheActivity: tela de detalhe do filme -->
        <activity
            android:name=".DetalheActivity"
            android:exported="false"/>

    </application>

</manifest>